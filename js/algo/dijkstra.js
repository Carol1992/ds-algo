/**
 * for a given source node in the graph, the algorithm finds the shortest path between the source node and every other nodes;
 * it can also be used for finding the shortest path from a single node to a single destination node
 * by stopping the algorithm once the shortest path to the destination node has been determined.
 * the working principle behind link-state routing protocols, OSPF, IS-IS
 * 
 * 
 */
